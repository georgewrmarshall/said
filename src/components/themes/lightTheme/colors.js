import { colors } from '../colors';

export const colorsTheme = {
  primaryLight: colors.blueLightest,
  primary: colors.blueLight,
  primaryDark: colors.blueDark,
  headerFontColor: colors.white,
  bodyFontColor: colors.white,
  bodyBackgroundColor: colors.blueLight,
  white: colors.white,
  grayLight: colors.grayLight,
};
